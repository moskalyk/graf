use define 0.7.19

constract Graf {
  dfine() // define wisdom, with whl embed nft
  spur() // place first light as program counter
  kiel() // complete closing with private hash
  iota() // complete quest with reward
  twice() // after 2 cycles completes, crystal airdrop
}
